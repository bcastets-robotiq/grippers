# Examples

**Start here: `move_gripper`** — the pattern every application should
use: a `Gripper` owns the bus; control code calls instant
setters and getters.

- `move_gripper` — activate, close, and open a gripper through
  `Gripper`'s typed accessors.
